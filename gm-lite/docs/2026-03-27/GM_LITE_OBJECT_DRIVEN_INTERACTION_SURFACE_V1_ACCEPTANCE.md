# GM_LITE_OBJECT_DRIVEN_INTERACTION_SURFACE_V1_ACCEPTANCE

## Acceptance

This line passes only if it delivers all of the following:

1. A visible task surface bound to `TaskArtifact`.
2. A visible run surface bound to `RunState`.
3. A visible next-action surface bound to `NextAction`.
4. A minimal visible defect / repair / re-entry path.
5. A minimal visible human-control surface bound to `HumanControlPoint`.
6. Evidence that the plugin is moving away from text/status-shell behavior.

If the result remains mostly text output or status inspection only, this line is `REQUIRES_CHANGES`.

