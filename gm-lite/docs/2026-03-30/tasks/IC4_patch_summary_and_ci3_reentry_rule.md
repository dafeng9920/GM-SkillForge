你是任务 `IC4` 的执行者 Kior-B。

你只做 execution，不做 review，不做 compliance。

权威项目树以 `D:\gm-lite` 为准。

唯一目标：
- 汇总 `IC1-IC3` 的修复结果
- 明确 `CI3` 重入时必须检查的证据和判断口径

必须写入：
- `gm-lite/docs/2026-03-30/verification/gm_lite_context_continuity_integration_caller_patch/IC4_execution_report.md`

必须包含：
1. `task_id: IC4`
2. `executor`
3. `PASS / REQUIRES_CHANGES / FAIL`
4. patch summary / CI3 re-entry rule 结论
5. 最少 `EvidenceRef`
