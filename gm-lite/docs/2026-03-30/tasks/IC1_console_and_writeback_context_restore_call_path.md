你是任务 `IC1` 的执行者 Antigravity-2。

你只做 execution，不做 review，不做 compliance。

权威项目树以 `D:\gm-lite` 为准。

唯一目标：
- 补齐 Console / writeback 路径对上下文恢复能力的实际调用
- 让回写读取后能自动恢复核心上下文，而不是只显示结果

必须写入：
- `gm-lite/docs/2026-03-30/verification/gm_lite_context_continuity_integration_caller_patch/IC1_execution_report.md`

必须包含：
1. `task_id: IC1`
2. `executor`
3. `PASS / REQUIRES_CHANGES / FAIL`
4. console / writeback context restore call path 结论
5. 最少 `EvidenceRef`
