你是任务 `IC2` 的执行者 Antigravity-1。

你只做 execution，不做 review，不做 compliance。

权威项目树以 `D:\gm-lite` 为准。

唯一目标：
- 补齐 followback / active surface 的上下文绑定
- 让返回工作面时能携带并恢复正确的 task / intent / continuity context

必须写入：
- `gm-lite/docs/2026-03-30/verification/gm_lite_context_continuity_integration_caller_patch/IC2_execution_report.md`

必须包含：
1. `task_id: IC2`
2. `executor`
3. `PASS / REQUIRES_CHANGES / FAIL`
4. followback / active surface context binding 结论
5. 最少 `EvidenceRef`
