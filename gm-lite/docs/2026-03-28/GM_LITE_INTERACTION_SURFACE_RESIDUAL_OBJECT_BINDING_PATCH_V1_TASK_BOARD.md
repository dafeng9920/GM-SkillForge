# GM_LITE_INTERACTION_SURFACE_RESIDUAL_OBJECT_BINDING_PATCH_V1_TASK_BOARD

## Release Order

First wave parallel:

- `RB1`
- `RB2`

Second wave serial:

- `RB3`

## Completion Rule

Codex only declares this patch line complete when:

- `RB1-RB3` all have `execution / review / compliance`
- the `IS4` residual findings are directly addressed
- `MinimalPanel` is no longer the primary status-shell fallback

