# TASK-MAIN-01-RETRY 蓝图

## 任务目标

将 `check_akshare_funcs.py` 接入本地主链，完成一次真实的任务承接。

## 选定任务

**文件**: `check_akshare_funcs.py`
**位置**: 仓库根目录
**风险等级**: LOW
**模块范围**: 单模块（工具脚本）

## 任务内容

### 当前状态
- 简单的 AKShare 函数列表工具
- 输出格式为纯文本
- 无 CLI 参数支持

### 计划变更
1. 添加结构化输出（JSON 格式选项）
2. 添加 CLI 参数（--format, --category）
3. 改进分组显示
4. 添加文档字符串

## 验收标准

1. ✅ 代码修改完成且向后兼容
2. ✅ `--help` 参数正常工作
3. ✅ 默认行为保持不变
4. ✅ JSON 输出格式正确

## 不变更项

- 不修改其他文件
- 不改变核心功能
- 不添加新依赖
