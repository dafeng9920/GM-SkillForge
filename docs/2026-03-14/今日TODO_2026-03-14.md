# 今日TODO · 2026-03-14

## 一、今日完成

今天的核心不是继续修页面细节，而是把前端主骨架进一步收拢，并把后端 orchestration 从“接口缝”推进到“开始承接真实对象”。

### 1. 前端主骨架继续收敛

- 统一输入链已经成立：
  - `GovernComposer`
  - `GovernanceInteractionProvider`
  - `turn / decision / activeDecision`
- 共享画布宿主已经成立：
  - `GovernanceCanvasSlotProvider`
  - `GovernanceCanvasSlotRenderer`
  - `ContextCanvasHost`
- `Home / Dashboard / Vetting / Audit Detail / Permit` 已经开始服从同一条输入 -> 状态机 -> 画布链。

### 2. 共享画布进入 payload-first

- `ContextCanvas` 已经不再主要依赖前端本地语义。
- 后端 `decision.canvasPayload` 现在是画布主数据源。
- 前端本地 registry 退为 fallback。

### 3. 三条主链已输入驱动

已打通三条输入驱动主链：

- `external_skill_vetting`
- `revision_audit`
- `permit_release`

这三条链现在都会根据输入内容动态变化，而不是只显示固定示例值。

### 4. 后端 orchestration 已开始返回真实承接对象

`/api/v1/governance/orchestrate` 现在除了返回 `decision`，还会返回：

- `canvasPayload`
- `trace.run_id`
- `trace.trace_id`
- `artifact_refs`
- `run_id`

同时 `canvasPayload` 里已经开始出现更接近真实业务的结构：

- `detailItems`
- `artifactItems`
- `actionItems`

### 5. 业务对象进一步真实化

后端当前已经开始从输入中提取：

- `asset_name`
- `revision`
- `source_type`
- `fingerprint`

并把这些对象映射到：

- `assetId`
- `reportId`
- `permitId`
- `packId`

现在画布里已经不只是状态说明，而开始承接“当前产物”和“待执行动作”。

### 6. 验证状态

今日关键验证已通过：

- `python -m py_compile d:\GM-SkillForge\simple_api.py d:\GM-SkillForge\skillforge\src\api\l4_api_fixed.py`
- `npm run build`

结论：

- 前端主骨架可用
- 前后端主链未被打坏
- 当前里程碑稳定

---

## 二、当前判断

### 1. 前端可以告一段落

不是说前端 100% 做完了，而是说：

- 前端主架构已经基本定型
- 继续深抠前端页面细节，边际收益开始下降
- 现在最值钱的工作重心，应当转向后端收拢

### 2. 现在还不是“L3 完成”

当前状态是：

- 已非常接近 L3 主骨架成立
- 但仍未等于完整 L3

差距主要在后端：

- orchestration 还偏规则驱动
- capability router 还未真正统一
- 真执行 / 真回写 / 真状态闭环还没打透
- 异步执行与次日验收模式还没实装
- deny / revoke / invalidate / drift / retry 等失败态还未彻底闭环

### 3. 今日结论

今天不是“系统做完了”，而是：

**前端主骨架基本成形，后端主战场已经清晰，系统已经进入最接近成功的一次。**

---

## 三、明日主战线

明天不再散着改页面，直接进入“后端收拢期”。

### 优先级 1：orchestration contract 真收口

目标：

- 把 `/api/v1/governance/orchestrate` 从“规则驱动返回”继续推进到“真实能力编排入口”
- 明确：
  - 输入
  - 状态机裁决
  - capability router
  - 画布 payload
  - trace / artifact refs / run_id
  之间的唯一真链路

### 优先级 2：capability router 收口

先收三条主链：

- `external_skill_vetting`
- `revision_audit`
- `permit_release`

目标不是再补说明文字，而是明确：

- 谁真正执行
- 谁真正返回
- 谁负责回写
- 谁负责失败态升级

### 优先级 3：真实执行 / 回写 / 状态闭环

把系统从：

- “前端承接后端判断”

推进到：

- “后端执行 -> 结果回写 -> 前端画布承接”

重点关注：

- artifact ids
- report ids
- permit ids
- trace ids
- run ids
- revision binding

### 优先级 4：异步执行模式落地准备

围绕已经打通的本地 -> 云端桥，开始准备真正的工业化模式：

- 本地提交
- 云端执行
- 夜间跑任务
- 次日验收

也就是把：

**“本地写 skill，云端 OpenClaw 执行，第二天回来验收”**

从桥接策略，逐步推进成真实系统能力。

### 优先级 5：失败态与失效链

明天后端若还有余力，要开始补这些：

- clarify
- deny
- revoke
- invalidate
- drift
- retry

因为这部分决定系统是不是治理系统，而不只是 happy path demo。

---

## 四、明日执行原则

### 1. 不再回到页面零散修补

除非后端契约改动必须带前端配合，否则不再把时间浪费在零散 UI 微调上。

### 2. 主火力打后端

前端进入：

- 维持
- 配合
- 小修

后端进入：

- 主攻
- 收口
- 打真链

### 3. 继续坚持主骨架

主骨架不变：

- 统一输入
- 状态机裁决
- orchestration 编排
- 共享画布承接
- 审计与状态回写

不再摇摆。

---

## 五、一句话总结

今天完成的是：

**前端主骨架基本定型，后端收拢入口已经打开。**

明天要做的是：

**集中兵力，正式挥师后端，把 orchestration / capability router / 执行回写闭环狠狠干穿。**
