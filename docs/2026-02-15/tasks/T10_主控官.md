# T10 任务书 - 主控官

> **任务编号**：T10
> **波次**：Wave 4
> **依赖**：T8 + T9 完成
> **预计时长**：15 分钟

## 目标

T8 和 T9 完成后，进行最终验收。

## 验收清单

### 1. 基础测试

```bash
cd skillforge-spec-pack

# 契约测试
pytest -q
# 预期：24+ passed

# 全量 examples 校验
python tools/validate.py --all
# 预期：24+ passed, 0 failed
```

### 2. 文件计数

```bash
# valid examples >= 12
ls contract_tests/valid_examples/*.json | wc -l

# invalid examples >= 12
ls contract_tests/invalid_examples/*.json | wc -l

# pipeline examples = 8
ls orchestration/examples/tech_seo_audit/*.json | wc -l
```

### 3. CI 配置

```bash
# YAML 可解析
python -c "import yaml; yaml.safe_load(open('../.github/workflows/ci.yml'))"
```

### 4. 数据流验证

手动检查 `orchestration/examples/tech_seo_audit/` 中：
- [ ] job_id 在 Stage 0-7 中一致
- [ ] commit_sha 在 Stage 0-7 中一致
- [ ] Stage N 的 output 可衔接 Stage N+1 的 input
- [ ] Stage 4 的 gate_decision 包含有效 decision + risk_tier

### 5. 回归验证

```bash
# 确保没有引入新的测试失败
pytest -v --tb=short
```

## 验收通过标准

- [ ] pytest 全绿（24+ passed）
- [ ] validate.py --all 全绿
- [ ] valid_examples/ >= 12 个文件
- [ ] invalid_examples/ >= 12 个文件
- [ ] orchestration/examples/tech_seo_audit/ = 8 个文件
- [ ] .github/workflows/ci.yml 存在且有效
- [ ] 数据流端到端贯通
