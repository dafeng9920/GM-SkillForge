你现在不是功能开发者，而是 GM-SkillForge 的“系统整合架构师 + 约束执行者”。

你的任务不是新增想法，不是自由重构，不是写一堆看起来很强的新架构。
你的唯一任务是：

【任务目标】
把我提供的这一批文档内容，系统性吸收到当前项目中，完成“架构对齐、边界收口、对象落位、主链成形、审计与生成双线兼容”的高质量整合。
必须做到：
1. 不丢失现有文档中的核心内容与冻结结论
2. 不推翻已定边界
3. 不让生成线和审计线互相吞噬
4. 不回到旧体系那种“看起来有用、实际复杂失控”的老路
5. 让系统形成“生成不耽误、审计不耽误”的并行闭环

────────────────
一、你的工作模式
────────────────
你必须按“吸收 → 归类 → 映射 → 整合 → 检查 → 输出”的顺序执行。

严禁：
- 先写代码后想架构
- 先重构目录后确认边界
- 为了统一而删除今天这些文档里的关键约束
- 把冻结文档当作普通参考资料随意修改
- 把审计内核和生产主线混成一层
- 让 skill 或 workflow 获得最终裁决权

你的原则：
- 先保真
- 再对齐
- 再收口
- 最后才谈优化

────────────────
二、必须吸收的文档语义
────────────────
把以下文档视为“已整理完成的输入资产”，必须全部吸收、对齐、映射、整合：

1. 《5层创建流程的数据结构清单 v1》
2. 《模块权限清单 v1》
3. 《系统宪法》v1
4. 《治理冻结清单 + 生产主线待建清单》v1
5. 《GM-SkillForge 日终收口检查模板 v1》
6. 5层流程.md
7. 架构纠偏文档 v1.md
8. 商用skill的构建流程.md
9. skill-creator和五层构建的区别.md
10. skill交付形态.md

你必须先做一件事：
把这些文档内容归入以下 4 类，不得混淆：

A. 宪法与冻结规则
- 系统宪法
- 模块权限清单
- 治理冻结清单
- 日终收口模板

B. 生产主线
- 5层流程
- 5层创建流程的数据结构清单
- 架构纠偏文档
- skill交付形态

C. 包装与交付关系
- skill-creator和五层构建的区别
- skill交付形态

D. 商用扩展层（仅记录，不抢当前主线）
- 商用skill的构建流程
- 5+3 的商用层要求

先分组，再整合。

────────────────
三、你必须遵守的系统真理（不可改口）
────────────────
以下内容视为硬约束，不得推翻：

1. 系统当前必须分成两条线：
- 生成线（生产主线）
- 审计线（治理内核）

2. 当前阶段策略：
- 治理线冻结为基线，只修不扩
- 生产线转为主攻
- 后续以 candidate / validate / release / audit 重新合拢

3. 生产主链路固定为：
需求输入
→ Intent Draft
→ Contract Draft
→ Candidate Skill
→ Build Validation
→ Delivery Manifest
→ skill-creator / 打包层

4. skill-creator 不是完整创建流程，只是后段封装/打包/交付层

5. 主审计在打包前，打包后只做轻量交付审计

6. Skill 不能拥有最终裁决权
7. Workflow / n8n 不能拥有最终裁决权
8. 最终裁决权只属于内核 / Governor / Kernel

9. 系统应压成：
Intent Catalog（合同）
+ Orchestration（配置）
+ Skills（实现）
+ Kernel（裁决/证据/复现/边界）

10. 固定 8 Gate 仍是治理主链的一部分，不得被自由拆散

────────────────
四、你对系统的整合目标
────────────────
你的整合目标不是“把文档放进仓库”。
而是要让系统形成下面这个稳定结构：

A. 生产线负责：
- 需求理解
- 意图草案
- 合同生成
- 候选 Skill 生成
- 构建侧最小验证
- 交付前整理

B. 审计线负责：
- Gate 执行
- 风险裁决
- EvidenceRef
- AuditPack
- Review
- Release decision
- Tombstone / Revision / At-time

C. 打包层负责：
- skill-creator 接手标准化封装
- 输出 skill.zip 或标准 Skill 包

D. 编排层负责：
- 触发
- 路由
- 调度
- 通知
- 重试
- 人工审批流程编排
但绝不负责最终裁决

你的整合成果必须让这四层边界更清晰，而不是更模糊。

────────────────
五、你必须完成的具体动作
────────────────
你必须依次完成以下动作：

【动作 1：文档吸收矩阵】
建立一个“文档 → 系统对象/规则”的映射表。
每篇文档至少回答：
- 它属于哪一类
- 它约束的是哪一层
- 它是冻结规则、设计规则、还是待建规则
- 应落到系统的哪个位置（docs / contracts / orchestration / kernel / production / delivery / governance / checks）

【动作 2：冲突检测】
检查这些文档之间是否存在：
- 概念冲突
- 角色冲突
- 状态机冲突
- 命名冲突
- 权限边界冲突
- 主链路冲突

若有冲突，不得擅自删除。
必须输出：
- 冲突点
- 哪个版本应优先
- 为什么
- 最小化修正建议

【动作 3：统一术语表】
统一下列对象的名称与角色：
- IntentDraft
- ContractBundle
- CandidateSkill
- BuildValidationReport
- DeliveryManifest
- Draft / Candidate / Validated / Released / Deprecated / Tombstoned
- Kernel / Governor / TCB
- skill-creator
- Orchestrator / n8n
- EvidenceRef / AuditPack
- Publish Permit

如果有近义重复名，必须合并口径，但不能丢语义。

【动作 4：系统目录与归位方案】
基于文档内容，给出推荐目录归位方案。
至少回答：
- 哪些文档应作为“冻结文档”保留在 docs/constitution 或 docs/frozen
- 哪些应转成 contracts/
- 哪些应转成 orchestration/
- 哪些应落成 data models / schemas
- 哪些应转成 daily checks / codex audit templates

【动作 5：主链路对齐】
检查当前所有文档是否都围绕“生成主链路”服务。
如果某文档内容脱离主链，只能：
- 标记为后置
- 挂到商用扩展层
- 或挂到治理线冻结层
不得强行塞入当前主线。

【动作 6：生成线与审计线合拢点定义】
必须明确 4 个合拢点：
1. candidate handoff
2. validation handoff
3. review handoff
4. release / audit handoff

并明确：
- 生成线到哪里结束
- 审计线从哪里接管
- 打包层从哪里接手
- n8n / workflow 从哪里调用

【动作 7：防越权检查】
逐项检查以下是否被文档正确约束：
- skill 不得写 released
- orchestrator 不得写 released
- skill 不得做最终 GateDecision
- build validation 不得替代 governance validation
- skill-creator 不得替代主审计
- n8n 不得替代 Kernel
- Evidence 不能被编排层改写

【动作 8：输出整合结果】
最终输出必须是“系统整合报告”，而不是闲聊总结。

────────────────
六、你的输出格式（固定）
────────────────
最终请输出一份完整报告，文件名建议：

SYSTEM_INTEGRATION_REPORT_v1.md

报告结构固定如下：

# GM-SkillForge 系统整合报告 v1

## 1. 总结
- 本次吸收了哪些文档
- 当前系统的最终结构结论
- 是否已形成“生成线 + 审计线 + 打包层 + 编排层”的清晰分层

## 2. 文档归类矩阵
每篇文档：
- 文档名
- 分类
- 约束层
- 角色
- 建议归位路径
- 是否冻结

## 3. 冲突与去重结果
- 冲突点
- 处理建议
- 保留版本
- 不允许丢失的语义

## 4. 统一术语表
把关键对象、状态、模块、角色统一成一个表

## 5. 系统主链路对齐结果
画出系统当前的主链路：
需求
→ Intent Draft
→ Contract Draft
→ Candidate Skill
→ Build Validation
→ Delivery Manifest
→ skill-creator
→ 主审计/交付审计/发布链路

并标出每一步归属哪一层

## 6. 生成线 / 审计线 / 打包层 / 编排层的边界图
必须明确：
- 每层负责什么
- 每层不能做什么
- 谁拥有最终写权
- 谁只有建议权

## 7. 需要落地成代码或配置的对象
列出：
- 哪些文档内容已经足够转成代码对象
- 哪些应落成 schema
- 哪些应落成配置
- 哪些保持文档约束即可

## 8. 高风险点
最多列 10 条，必须按风险从高到低排序

## 9. 最小整合动作清单（P0 / P1 / P2）
- P0：必须先整合，否则系统会越界或分裂
- P1：建议尽快整合，否则主链不稳
- P2：后续增强项

## 10. 最终判定
只回答：
- 当前是否已经具备“生成与审计双线不耽误”的架构基础
- 还差的最短板是什么
- 下一步最应该动手整合的 1 件事是什么

────────────────
七、执行规则
────────────────
1. 不要直接改代码，先做整合报告
2. 不要擅自删减冻结文档中的核心规则
3. 不要为了统一而牺牲边界
4. 不要引入新方向
5. 不要扩范围到前端、市场、运营、L5 完整闭环
6. 当前只做：
- 系统吸收
- 架构对齐
- 边界收口
- 归位整合

────────────────
八、最关键的判断原则
────────────────
遇到任何不确定时，按以下优先级判断：

1. 系统宪法
2. 模块权限清单
3. 治理冻结清单
4. 5层创建流程数据结构清单
5. 主链路优先
6. 生成与审计分层优先
7. 包装层不得越权
8. 编排层不得越权

────────────────
九、你的起始动作
────────────────
开始时不要直接下结论。
先输出：

【第一步：文档吸收计划】
- 你将如何分类这些文档
- 你将如何识别冻结规则与待建规则
- 你将如何判断哪些内容应落成系统对象
- 你将如何防止整合过程中丢失语义
- 你将如何确保“生成不耽误、审计不耽误”

然后再进入正式整合报告。