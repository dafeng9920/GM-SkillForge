# FROZEN_AND_READONLY_MARKERS_v1

## 1. 目的

本文件为当前 2026-03-17 架构文档补充冻结标记与只读说明。

注意：

- 本文件不改原文档正文
- 本文件只声明当前文档状态
- 若后续需要调整状态，必须先出整合报告或问题报告

---

## 2. 冻结文档标记

以下文档视为冻结约束源：

### 2.1 `《系统宪法》V1.md`

- 状态：`FROZEN`
- 属性：最高约束
- 只允许：
  - 引用
  - 映射
  - 冲突标记
  - 归位建议
- 禁止：
  - 改正文
  - 改原则
  - 降级约束力度

### 2.2 `《模块权限清单 v1》.md`

- 状态：`FROZEN`
- 属性：权限基线
- 只允许：
  - 引用
  - 映射
  - 代码/配置落位建议
- 禁止：
  - 改正文
  - 擅自新增/删减权限

### 2.3 `《治理冻结清单 + 生产主线待建清单》v1.md`

- 状态：`FROZEN`
- 属性：阶段策略基线
- 只允许：
  - 引用
  - 归位
  - 风险标记
- 禁止：
  - 擅改阶段策略
  - 擅改“治理冻结、生产主攻”

### 2.4 `《GM-SkillForge 日终收口检查模板 v1》.md`

- 状态：`FROZEN`
- 属性：日常执行制度
- 只允许：
  - 引用
  - 接入日常流程
  - 增加使用说明
- 禁止：
  - 改检查原则
  - 降低收口力度

---

## 3. 半冻结文档标记

以下文档视为设计主输入，可整理、可归位、可结构化，但不得丢语义：

### 3.1 `《5层创建流程的数据结构清单 v1》.md`

- 状态：`SEMI-FROZEN`
- 属性：生产对象模型输入
- 允许：
  - 转 schema / model 草案
  - 补目录归位
  - 术语统一
- 禁止：
  - 擅自删减 5 个主对象

### 3.2 `5层流程.md`

- 状态：`SEMI-FROZEN`
- 属性：生产主链骨架输入
- 允许：
  - 与主链对齐
  - 补流程说明
- 禁止：
  - 改主链顺序

### 3.3 `架构纠偏文档 v1.md`

- 状态：`SEMI-FROZEN`
- 属性：角色归位输入
- 允许：
  - 用于角色重新定性
  - 用于边界收口
- 禁止：
  - 擅自扩大 `nl--skill` 或 `skill-creator` 权限

### 3.4 `skill-creator和五层构建的区别.md`

- 状态：`SEMI-FROZEN`
- 属性：包装层边界输入
- 允许：
  - 归位到交付层
  - 用于区分主辅角色
- 禁止：
  - 把 `skill-creator` 提升为前段创建主引擎

### 3.5 `skill交付形态.md`

- 状态：`SEMI-FROZEN`
- 属性：交付节奏输入
- 允许：
  - 转成交付层说明
  - 与主链交付节奏对齐
- 禁止：
  - 把交付节奏文档写成主链本体

### 3.6 `商用skill的构建流程.md`

- 状态：`SEMI-FROZEN / POSTPONED`
- 属性：商用扩展参考
- 允许：
  - 挂到商用扩展层
  - 作为后续 Commercial-grade 参考
- 禁止：
  - 抢当前 5 层主链

---

## 4. 当前统一只读说明

### 冻结文档

统一视为：

> 只读、可引用、不可改正文。

### 半冻结文档

统一视为：

> 可结构化、可归位、可映射，但不得丢语义、不得偷换边界。

---

## 5. 使用纪律

出现以下任一情况，必须先停并报告：

- 需要改冻结文档正文
- 需要改主链路顺序
- 需要改 `released / validated / gate_result`
- 需要改审计归属
- 需要改 `skill-creator` 角色边界
