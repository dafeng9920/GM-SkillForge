# 任务 A5: Trend Following Strategy

## 执行者
**Kiro-2**

---

## 任务概述
迁移 `trend_following` 策略意图，从 NEW-GM 到 SkillForge。

---

## 跨项目路径

| 类型 | 路径 |
|------|------|
| **源项目（只读）** | `D:\NEW-GM` |
| **目标目录（只写）** | `D:\GM-SkillForge\docs\2026-02-17\图书馆迁移` |
| **源文件参考** | `D:\NEW-GM\strategies\template_trend_following.py` |
| **最终目标** | `skillforge/src/skills/strategies/trend_following.py` |

---

## 硬约束（必须遵守）

1. **不复制任何代码**，只迁移意图语义（intent semantics）
2. 每条结论必须附 `source_doc_ref` 与 `evidence_ref`
3. 输出必须可用于 contracts-first + Gate + EvidenceRef + L3 AuditPack 映射
4. 无证据结论一律 **reject**

---

## 执行步骤

### Step 1: 源文件扫描
- 读取 `D:\NEW-GM\strategies\template_trend_following.py`
- 提取核心策略意图：
  - MA Crossover（均线交叉）
  - ATR Trailing Stop（ATR 跟踪止损）

### Step 2: 定义合约
在目标目录创建：
```
contracts/intents/trend_following.yml
```

必须包含：
- `intent_name`: trend_following
- `input_schema`: MarketData (OHLCV + Indicators)
- `output_schema`: Signal (LONG/SHORT/FLAT + Confidence)
- `evidence_required`: BacktestRef（模拟回测引用）

### Step 3: 核心逻辑映射
```
Input: MarketData
Process:
  1. 计算 MA Crossover 信号
  2. 应用 ATR Trailing Stop 风控
Output: Signal

Evidence: BacktestRef (Simulated)
```

### Step 4: 输出文件
在 `D:\GM-SkillForge\docs\2026-02-17\图书馆迁移` 目录输出：

```
A5_scan.md
```

字段必须包含：
| 字段 | 说明 |
|------|------|
| `component_id` | 组件唯一标识 |
| `source_doc_ref` | 源文件路径 + 行号引用 |
| `intent_summary` | 意图摘要（中文） |
| `mapping_target` | 映射目标路径 |
| `value_score` | 价值评分 (1-10) |
| `risk_level` | 风险等级 (Low/Medium/High) |
| `migration_decision` | 迁移决策 (MIGRATE/DEFER/REJECT) |
| `evidence_ref` | 证据引用 |

---

## 策略核心概念

**MA Crossover**:
- 快速均线上穿慢速均线 → 看多信号
- 快速均线下穿慢速均线 → 看空信号

**ATR Trailing Stop**:
- 基于 ATR（平均真实波幅）设置动态止损
- 保护利润，控制下行风险

---

## 完成标准

- [ ] A5_scan.md 已写入目标目录
- [ ] contracts/intents/trend_following.yml 已定义
- [ ] MA Crossover + ATR Trailing Stop 意图已映射
- [ ] 所有结论都有 source_doc_ref + evidence_ref
- [ ] 可直接进入 SkillForge Wave 4 Batch 1 执行

---

## 汇报格式

任务完成后，向**迁移总控**汇报：
```
【A5 完成】
- 输出文件：A5_scan.md
- 迁移决策：MIGRATE / DEFER / REJECT
- 关键发现：（3-5条）
- 阻塞问题：（如有）
```

---
*Generated by 迁移总控 | Wave 4 Batch 1*
