# 任务 A3: Upgrade Skill

## 执行者
**VSCode-3**

---

## 任务概述
迁移 `upgrade_skill` 技能意图，从 NEW-GM 到 SkillForge。

---

## 跨项目路径

| 类型 | 路径 |
|------|------|
| **源项目（只读）** | `D:\NEW-GM` |
| **目标目录（只写）** | `D:\GM-SkillForge\docs\2026-02-17\图书馆迁移` |
| **源文件参考** | `D:\NEW-GM\scripts\update_revision.py` |
| **最终目标** | `skillforge/src/skills/upgrade_skill.py` |

---

## 硬约束（必须遵守）

1. **不复制任何代码**，只迁移意图语义（intent semantics）
2. 每条结论必须附 `source_doc_ref` 与 `evidence_ref`
3. 输出必须可用于 contracts-first + Gate + EvidenceRef + L3 AuditPack 映射
4. 无证据结论一律 **reject**

---

## 执行步骤

### Step 1: 源文件扫描
- 读取 `D:\NEW-GM\scripts\update_revision.py`
- 提取核心意图：版本递增逻辑、evolution.json 结构

### Step 2: 定义合约
在目标目录创建：
```
contracts/intents/upgrade_skill.yml
```

必须包含：
- `intent_name`: upgrade_skill
- `input_schema`: SkillID + UpgradeSpec
- `output_schema`: UpgradeManifest
- `validation_rules`: 原子更新（Atomic update only）

### Step 3: 核心逻辑映射
```
Input: SkillID + UpgradeSpec
Process:
  1. 递增 revision 版本号
  2. 追加记录到 evolution.json
Output: UpgradeManifest

Constraint: 原子更新（Atomic update only）
```

### Step 4: 输出文件
在 `D:\GM-SkillForge\docs\2026-02-17\图书馆迁移` 目录输出：

```
A3_scan.md
```

字段必须包含：
| 字段 | 说明 |
|------|------|
| `component_id` | 组件唯一标识 |
| `source_doc_ref` | 源文件路径 + 行号引用 |
| `intent_summary` | 意图摘要（中文） |
| `mapping_target` | 映射目标路径 |
| `value_score` | 价值评分 (1-10) |
| `risk_level` | 风险等级 (Low/Medium/High) |
| `migration_decision` | 迁移决策 (MIGRATE/DEFER/REJECT) |
| `evidence_ref` | 证据引用 |

---

## 完成标准

- [ ] A3_scan.md 已写入目标目录
- [ ] contracts/intents/upgrade_skill.yml 已定义
- [ ] 原子更新约束已映射
- [ ] 所有结论都有 source_doc_ref + evidence_ref
- [ ] 可直接进入 SkillForge Wave 4 Batch 1 执行

---

## 汇报格式

任务完成后，向**迁移总控**汇报：
```
【A3 完成】
- 输出文件：A3_scan.md
- 迁移决策：MIGRATE / DEFER / REJECT
- 关键发现：（3-5条）
- 阻塞问题：（如有）
```

---
*Generated by 迁移总控 | Wave 4 Batch 1*
