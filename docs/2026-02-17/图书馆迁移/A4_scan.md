# A4_scan.md - Tombstone Skill 迁移扫描报告

**执行者**: Kiro-1
**扫描时间**: 2026-02-18
**任务**: A4 Tombstone Skill 迁移

---

## 1. 组件扫描结果

| 字段 | 值 |
|------|------|
| `component_id` | `intent-tombstone-skill-v0.1` |
| `source_doc_ref` | `D:\NEW-GM\AUDIT\CONSTITUTION.md#L12-L16` (Section 3: Default Deny) |
| `intent_summary` | 实现 Fail-Closed 原则下的技能永久废弃机制。系统在启动、失败或不确定时默认为 REVOKED 状态，任何歧义导致 DENY 裁决，废弃操作不可逆。 |
| `mapping_target` | `D:\GM-SkillForge\docs\2026-02-17\图书馆迁移\contracts\intents\tombstone.yml` → `skillforge/src/skills/tombstone.py` |
| `value_score` | 10/10 (核心安全约束，不可缺失) |
| `risk_level` | High (涉及生产数据永久变更，不可逆操作) |
| `migration_decision` | **MIGRATE** |
| `evidence_ref` | `CONSTITUTION-DEFAULT-DENY-L12-16`, `MIGRATION-PLAN-TOMBSTONE-L86`, `3DRAG-TOMBSTONE-L13-17` |

---

## 2. 源文档证据提取

### 2.1 CONSTITUTION.md - Section 3: Default Deny

**文件路径**: `D:\NEW-GM\AUDIT\CONSTITUTION.md`
**行号**: 12-16
**证据ID**: `CONSTITUTION-DEFAULT-DENY-L12-16`

**原文**:
```markdown
## 3. The "Default Deny" Principle
*   **State**: The system defaults to `REVOKED` on startup, failure, or uncertainty.
*   **Action**: Any ambiguity results in a `DENY` verdict.
*   **Traffic**: No order leaves the system without an explicit, valid `PermitToken`.
```

**提取意图**:
- 系统默认状态为 `REVOKED`
- 任何不确定性导致 `DENY` 裁决
- Fail-Closed 原则：宁可拒绝，不可误放

---

### 2.2 图书馆意图迁移执行方案_v2.md

**文件路径**: `D:\NEW-GM\docs\2026-02-17\图书馆意图迁移执行方案_v2.md`
**行号**: 86
**证据ID**: `MIGRATION-PLAN-TOMBSTONE-L86`

**原文**:
```markdown
- `tombstone_skill`
```

**提取意图**:
- `tombstone_skill` 被列为核心意图之一，需首批迁移

---

### 2.3 三维RAG与数据飞轮约束_v1.md

**文件路径**: `D:\NEW-GM\docs\2026-02-17\三维RAG与数据飞轮约束_v1.md`
**行号**: 13-17
**证据ID**: `3DRAG-TOMBSTONE-L13-17`

**原文**:
```markdown
`3D RAG = at-time + tombstone + system-of-record`

1. `at-time`：任意结论可回到当时版本口径重放。
2. `tombstone`：下架/失效知识不可被检索链绕过。
3. `system-of-record`：检索与裁决依据必须可追溯到审计资产。
```

**提取意图**:
- `tombstone` 是 3D RAG 核心维度之一
- 废弃知识不可被检索链绕过（强约束）
- 必须有审计追溯能力

---

## 3. 核心逻辑映射

```
┌─────────────────────────────────────────────────────────────┐
│                    TOMBSTONE SKILL FLOW                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Input:                                                      │
│  ├── skill_id: string (被废弃技能ID)                         │
│  ├── revoke_reason: string (废弃原因)                        │
│  ├── revoked_by: string (操作者)                             │
│  └── revoke_timestamp: datetime (时间戳)                     │
│                                                              │
│  Process:                                                    │
│  ├── 1. intake_validate (输入校验)                           │
│  ├── 2. constitution_risk_gate (宪法风险门禁)                │
│  ├── 3. tombstone_write_gate (废弃写入门禁)                  │
│  │      └── 写入 REVOKED 记录到 tombstones.json              │
│  ├── 4. audit_chain_append (审计链追加)                      │
│  └── 5. tombstone_publish (发布废弃状态)                     │
│                                                              │
│  Output:                                                     │
│  ├── TombstoneRecord (废弃记录)                              │
│  ├── tombstone_state = REVOKED (固定状态)                    │
│  └── EvidenceRef (证据引用)                                  │
│                                                              │
│  Constraint: Fail-Closed                                     │
│  └── Once revoked, NEVER valid again                         │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. Fail-Closed 约束映射

| 约束ID | 约束名称 | 源文档引用 | 实现要求 |
|--------|----------|------------|----------|
| FC-001 | 默认 REVOKED | CONSTITUTION.md L13 | 系统启动/失败/不确定时状态为 REVOKED |
| FC-002 | 歧义即拒绝 | CONSTITUTION.md L14 | 任何歧义导致 DENY 裁决 |
| FC-003 | 不可逆废弃 | 3DRAG约束 L16 | 废弃操作不可撤销 |
| FC-004 | 检索拦截 | 3DRAG约束 L16 | 废弃技能不可被检索链绕过 |
| FC-005 | 恢复拒绝 | 任务定义 | 任何恢复请求必须被拒绝 |

---

## 5. 输出文件清单

| 文件 | 路径 | 状态 |
|------|------|------|
| 合约定义 | `contracts/intents/tombstone.yml` | ✅ 已创建 |
| 扫描报告 | `A4_scan.md` | ✅ 本文件 |

---

## 6. 完成标准检查

- [x] A4_scan.md 已写入目标目录
- [x] contracts/intents/tombstone.yml 已定义
- [x] Fail-Closed 约束已映射 (FC-001 ~ FC-005)
- [x] 所有结论都有 source_doc_ref + evidence_ref
- [x] 可直接进入 SkillForge Wave 4 Batch 1 执行

---

## 7. 迁移决策摘要

**决策**: MIGRATE

**理由**:
1. `tombstone_skill` 是核心安全意图，价值评分 10/10
2. Fail-Closed 原则是宪法级约束，不可缺失
3. 源文档证据完整，意图语义清晰
4. 是 3D RAG 架构的关键维度之一
5. 已被列入首批迁移清单

---

*Generated by Kiro-1 | Task A4 | 2026-02-18*
