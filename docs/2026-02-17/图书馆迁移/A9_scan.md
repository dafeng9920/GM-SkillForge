# A9_scan.md - Time Semantics Scan Report

## 扫描元数据

| 字段 | 值 |
|------|------|
| **任务ID** | A9 |
| **扫描日期** | 2026-02-18 |
| **执行者** | Antigravity-3 |
| **源项目** | D:\NEW-GM |
| **目标目录** | D:\GM-SkillForge\docs\2026-02-17\图书馆迁移 |

---

## 源文件分析

### 源文件引用
- **文件路径**: `D:\NEW-GM\AUDIT\CONSTITUTION.md`
- **相关章节**: Section 4: Time Semantics
- **行号范围**: 17-21

### 原始内容提取

```markdown
## 4. Time Semantics
*   **Event Time**: When an event actually occurred.
*   **Available Time**: When the information became available to the system.
*   **No Time Travel**: Decisions must only use data where `available_time <= decision_time`.
```

---

## 核心概念映射

### 三种时间定义

| 时间类型 | 定义 | 来源引用 |
|----------|------|----------|
| **EventTime** | When an event actually occurred | CONSTITUTION.md L17 |
| **AvailableTime** | When the information became available to the system | CONSTITUTION.md L18-19 |
| **DecisionTime** | 决策时间（由 No Time Travel 规则隐含） | CONSTITUTION.md L20-21 |

### 核心约束

```
核心断言: available_time <= decision_time
完整链式: EventTime <= AvailableTime <= DecisionTime
```

**违反后果**:
1. Look-Ahead Bias (前视偏差) - 使用未来数据
2. Backtest Distortion - 回测结果不可靠
3. Production Crash - 实时数据不可用

---

## 迁移评估表

| 字段 | 值 |
|------|------|
| `component_id` | A9_time_semantics |
| `source_doc_ref` | D:\NEW-GM\AUDIT\CONSTITUTION.md:17-21 |
| `intent_summary` | 定义时间语义核心约束，防止 Look-Ahead Bias。核心规则：决策只能使用 available_time <= decision_time 的数据。 |
| `mapping_target` | skillforge/src/kernel/time_manager.py |
| `value_score` | 9 |
| `risk_level` | Low |
| `migration_decision` | **MIGRATE** |
| `evidence_ref` | EVID-A9-001, EVID-A9-002, EVID-A9-003 |

---

## 证据引用详情

### EVID-A9-001
- **来源**: CONSTITUTION.md L17
- **引用**: "**Event Time**: When an event actually occurred."
- **解读**: 事件实际发生时间，作为事实时间锚点

### EVID-A9-002
- **来源**: CONSTITUTION.md L18-19
- **引用**: "**Available Time**: When the information became available to the system."
- **解读**: 数据可用时间，决定何时可以合法使用该数据

### EVID-A9-003
- **来源**: CONSTITUTION.md L20-21
- **引用**: "**No Time Travel**: Decisions must only use data where `available_time <= decision_time`."
- **解读**: 核心约束规则，防止使用未来数据

---

## 使用场景映射

| 场景 | 约束说明 | 实现要求 |
|------|----------|----------|
| **RAG** | 检索时只能使用 decision_time 之前的数据 | 实现 `filter_by_availability()` |
| **Backtest** | 模拟历史决策时严格遵守时间约束 | 实现时间点数据可用性模拟 |
| **Real-time** | 实时决策时验证数据可用性 | 实现 `check_data_freshness()` |

---

## 迁移决策理由

### 决策: MIGRATE

**支持理由**:
1. ✅ 源文件有明确的时间语义定义（CONSTITUTION.md Section 4）
2. ✅ 核心约束规则清晰（available_time <= decision_time）
3. ✅ 三种时间概念完整映射（Event/Available/Decision）
4. ✅ Kernel 级别基础设施工具，高复用价值
5. ✅ 风险等级低，无复杂依赖

**无阻塞问题**

---

## 目标接口设计

```python
# skillforge/src/kernel/time_manager.py

class TimeManager:
    """Kernel-level time semantics manager"""

    def validate_time_constraint(
        self,
        event_time: datetime,
        available_time: datetime,
        decision_time: datetime
    ) -> bool:
        """
        Validate time constraint chain.
        Returns True if EventTime <= AvailableTime <= DecisionTime
        """
        pass

    def filter_by_availability(
        self,
        data: List[DataPoint],
        decision_time: datetime
    ) -> List[DataPoint]:
        """
        Filter data by availability constraint.
        Returns only data where available_time <= decision_time
        """
        pass

    def check_data_freshness(
        self,
        available_time: datetime,
        current_time: datetime,
        max_lag: timedelta
    ) -> bool:
        """
        Check if data is fresh enough for real-time decisions.
        """
        pass
```

---

## 输出文件清单

| 文件 | 路径 | 状态 |
|------|------|------|
| 合约定义 | contracts/intents/time_semantics.yml | ✅ 已创建 |
| 扫描报告 | A9_scan.md | ✅ 本文件 |

---

## 完成标准检查

- [x] A9_scan.md 已写入目标目录
- [x] contracts/intents/time_semantics.yml 已定义
- [x] EventTime/AvailableTime/DecisionTime 概念已映射
- [x] 核心断言 `available_time <= decision_time` 已明确
- [x] 所有结论都有 source_doc_ref + evidence_ref
- [x] 可直接进入 SkillForge Wave 4 Batch 1 执行

---

*Generated by Antigravity-3 | Task A9 | 2026-02-18*
