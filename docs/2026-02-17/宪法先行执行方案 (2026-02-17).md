宪法先行执行方案 (2026-02-17)
我的意见：完全赞同
你的优先级调整是正确的。理由：

constitution_v1.md
 §2.5 明确写了"宪法先于能力"。如果我们先做 L5 Gate 试跑或 10阶 Skill，就违反了宪法自身的信条。
不可回退架构约束_v1.md §1.1 要求宪法必须版本化并写入 provenance / 
AuditPack
（constitution_ref + constitution_hash）。但当前引擎的 
pack_publish.py
 没有 constitution_ref 字段——这是个真实缺口。
不可回退架构约束_v1.md §6 定义了 V1-Prove 窗口（2/17 ~ 3/3）。今天是窗口第一天，正是固化宪法的最佳时机。
先建宪法层，让后续所有开发（A/B）都在宪法约束下运行，而不是回头补治理——这正是 §C.3 禁止的"先写功能后补治理"。
IMPORTANT

执行顺序调整为：宪法层 → L5 Gate → 10阶 Skill

Wave 0: 宪法层固化（今天，优先级最高）
0.1 把宪法文档正式入驻 GM-SkillForge
文件	来源	目标位置
constitution_v1.md
✅ 已同步	
docs/2026-02-16/constitution_v1.md
不可回退架构约束_v1.md	❌ 仅在 NEW-GM	需同步到 
docs/2026-02-16/不可回退架构约束_v1.md
skill_10x_cognition_execution_order_v1.md
❌ 仅在 NEW-GM	需同步到 docs/2026-02-16/
0.2 宪法在引擎中生效（代码层面）
根据 不可回退架构约束_v1.md §1.1 的要求，需要在引擎中实现：

constitution_ref 字段: 在 manifest.json / AuditPack 里嵌入宪法引用
文件: pack_publish.py — _build_provenance() 加入 constitution_ref + constitution_hash
constitution_risk_gate 检查: 现有 constitution_gate.py 需增加宪法版本校验
校验请求中是否包含 constitution_ref
校验 hash 是否匹配当前宪法
validate.py 增加宪法检查点:
CHECK 16: constitution_v1.md 文件存在
CHECK 17: manifest 包含 constitution_ref + constitution_hash
0.3 产出 V1-Prove 的首个证据
写一个测试故意违反宪法（如缺 constitution_ref），验证 REJECTED
这同时满足 不可回退架构约束 §6.4 的"至少 1 个真实 REJECTED 案例可追溯"
Wave 1: L5 Gate 干跑（宪法到位后）
即 TODO 的 Section A，但现在是在宪法约束下运行：

A1 + A4: 写 l5_dry_run.py，在 G1-G5 之前先检查 constitution_ref
A2: Gate 检查命令映射表
A3: 真实 PASSED / REJECTED 报告
A5: v3 引用替换清单
Wave 2: 10阶认知输出 Skill（契约冻结后）
即 TODO 的 Section B，严格按 Phase A→D 顺序：

Phase A: 术语统一 + 边界确认
Phase B: 契约冻结
Phase C: 审计面先行
Phase D: 启动门槛
今天具体可做的 Deliverables
Wave 0 (宪法层):
├── 0.1 同步 不可回退架构约束_v1.md + skill_10x_cognition 到 GM-SkillForge
├── 0.2a pack_publish.py 增加 constitution_ref + constitution_hash
├── 0.2b constitution_gate.py 增加宪法版本校验
├── 0.2c validate.py 增加 CHECK 16-17
├── 0.3 test_constitution_enforcement.py (REJECTED 证据)
└── 全套测试跑绿
Wave 1 (L5 Gate, 如果时间允许):
├── A5: v3 引用替换清单
└── A1+A4: l5_dry_run.py 干跑
审批要点
你同意"Wave 0 宪法先行"这个优先级吗？
不可回退架构约束_v1.md 需要你手动同步到 GM-SkillForge，还是我帮你复制？
Wave 0 的代码改动（constitution_ref 嵌入 manifest）需要先给你看方案还是直接动手？


