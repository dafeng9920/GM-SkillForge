任务 T-W0-A 完成
已交付文件
文件	类型	说明
utils/init.py	新建	utils 包初始化
utils/constitution.py	新建	load_constitution() 函数，返回 (constitution_ref, constitution_hash)
nodes/pack_publish.py	修改	集成宪法引用到 manifest.provenance
实现要点
load_constitution() 函数

从 docs/2026-02-16/constitution_v1.md 读取宪法文件
返回 (constitution_ref, constitution_hash) 元组
文件缺失时返回 ("MISSING", "")
hash 使用 SHA256 hex digest（64字符）
pack_publish.py 修改

导入 load_constitution 函数
在 execute() 开头调用并获取宪法引用
manifest.provenance 新增 constitution_ref 和 constitution_hash 字段
宪法缺失时强制 publish_status = "rejected"
质量门禁验证
检查项	状态
load_constitution() 返回正确值	✅
hash 长度为 64 字符	✅
manifest.provenance 包含宪法字段	✅
宪法缺失时返回 rejected	✅
L3 AuditPack 测试通过	✅
注意: pytest 有 12 个预先存在的失败（test_protocols.py 中的 NotImplementedError 
--------------
任务 T-W0-B 完成 ✓
已完成的修改
文件: constitution_gate.py

导入 load_constitution (L38)


from skillforge.src.utils.constitution import load_constitution
execute() 开头添加宪法加载和缺失检查 (L76-93)

调用 load_constitution() 获取 constitution_ref 和 constitution_hash
宪法缺失时立即返回 DENY，reason 引用 §2.2 Fail-Closed
替换所有硬编码 "0.1.0"

robots_txt violation (L154)
authenticated_access violation (L172)
最终返回 (L206)
全部改为使用真实的 constitution_ref
新增 constitution_hash 字段

所有返回的 details 中都包含 constitution_hash
更新 docstring 和 validate_output

输出合同文档增加 constitution_hash 字段说明
validate_output() 现在校验 constitution_hash 必须存在
测试结果
宪法相关测试: 19/19 通过 ✓
总测试: 204 passed, 12 failed
失败的 12 个测试是 test_protocols.py 中预先存在的问题（与本次修改无关）
满足的约束条件
✓ Gate 输出的 constitution_version 来自真实文件
✓ Gate 输出新增 constitution_hash 字段
✓ 宪法文件缺失时 decision == 'DENY'
✓ DENY 的 reason 明确引用宪法条款 (§2.2 Fail-Closed)
✓ 未修改 pack_publish.py / validate.py / 测试文件
✓ 未重新实现 load_constitution()，从 utils/constitution.py 导入
-----------
Task T-W0-D Completed ✓
Created test_constitution_enforcement.py with 5 tests covering constitution enforcement:

#	Test	Purpose
1	test_gate_deny_when_constitution_missing	Verifies fail-closed behavior - when constitution is missing, pack must be REJECTED with constitution_ref="MISSING"
2	test_gate_includes_constitution_hash	Validates constitution_hash in manifest provenance is SHA256 (64 hex chars)
3	test_manifest_includes_constitution_ref	Confirms manifest.provenance contains constitution_ref that is not "MISSING"
4	test_pack_rejected_when_constitution_missing	Explicitly tests pack rejection path when constitution unavailable
5	test_constitution_hash_is_deterministic	Ensures load_constitution() returns identical hashes on repeated calls
Gate Check Results
5 passed in test_constitution_enforcement.py ✓
13 passed in test_skill_threshold.py ✓
17 passed in acceptance + constitution tests ✓
Constraints Met
✓ At least 1 test covers REJECTED/DENY path (tests 1 & 4)
✓ Tests use pytest fixtures (monkeypatch, tmp_path) for isolation
✓ No modifications to pack_publish.py, constitution_gate.py, or validate.py
✓ No new third-party dependencies
✓ V1-Prove §6.4 satisfied: traceable REJECTED case exists
----------------
✅ 任务 T-W0-C 完成
已完成的修改
文件: validate.py

新增 CHECK 16 (CONSTITUTION_EXISTS) - 在第 483-492 行

校验 docs/2026-02-16/constitution_v1.md 存在性
新增 CHECK 17 (CONSTRAINTS_EXISTS) - 在第 494-503 行

校验 docs/2026-02-16/不可回退架构约束_v1.md 存在性
更新标题 - 第 506 行

"15-Point Config Validation" → "17-Point Config Validation"
更新 help text - 第 490-491 行

"12-point Audit Engine Protocol" → "17-point Audit Engine Protocol"
Gate Check 结果
检查项	结果
python tools/validate.py --audit-config	✅ 17 passed, 0 failed
audit 相关 pytest 测试	✅ 3 passed
红线遵守情况
✅ 未修改 pack_publish.py
✅ 未修改 constitution_gate.py
✅ 未修改已有的 CHECK 1-15 逻辑
✅ 未引入新的第三方依赖