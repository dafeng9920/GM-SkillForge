# 非 dry-run 发布前验证执行报告 v1

> **版本**: 1.0.0
> **执行时间**: 2026-02-18T11:24:03Z
> **run_id**: RUN-20260218-PRERELEASE-001
> **状态**: PASSED
> **关联运行单**: `docs/2026-02-18/prerelease_non_dry_run_validation_run_sheet_v1.md`

---

## 1. 执行摘要

本次验证完成 Permit 签发与校验的全链路测试，使用**真实 HS256 签名验证**（非 stub），验证结果：

| 检查项 | 结果 |
|--------|------|
| Permit 签发 | ✅ PASSED |
| Permit 校验 | ✅ VALID |
| 正常路径放行 | ✅ release_allowed=true |
| 无 permit 阻断 | ✅ E001 |
| 签名篡改阻断 | ✅ E003 |
| Fail-Closed 行为 | ✅ 正常 |
| **最终判定** | **PASSED** |

---

## 2. 输入冻结记录

| 字段 | 值 |
|------|-----|
| repo_url | `https://github.com/local/NEW-GM` |
| commit_sha | `4ea179d31a66fc2616f0cf953cd0e5c5c43d8ea8` |
| at_time | `2026-02-18T11:24:03Z` |
| run_id | `RUN-20260218-PRERELEASE-001` |
| intent_id | `generate_skill_from_repo` |
| audit_pack_ref | `audit-10465f76` |
| gate_profile | `batch2_8gate_with_permit` |
| requested_action | `release` |

---

## 3. 签发证据

### 3.1 签发结果

```
permit_id: PERMIT-20260218-DD5EFFFA
issued_at: 2026-02-18T11:24:03Z
expires_at: 2026-02-18T12:24:03Z
signature_algo: HS256
key_id: skillforge-permit-key-2026
```

### 3.2 签发 EvidenceRef

```
issue_key: PERMIT-ISSUE-PERMIT-20260218-DD5EFFFA-1771413843
source_locator: issuer://permit_issuer
tool_revision: 1.0.0
timestamp: 2026-02-18T11:24:03Z
decision_snapshot:
  check: issuance
  passed: true
  permit_id: PERMIT-20260218-DD5EFFFA
  expires_at: 2026-02-18T12:24:03Z
```

---

## 4. 校验证据

### 4.1 正常路径校验

```
validation_status: VALID
gate_decision: ALLOW
release_allowed: true
release_blocked_by: null
error_code: null
evidence_ref: PERMIT-VAL-PERMIT-20260218-DD5EFFFA-1771413843
```

### 4.2 无 permit 阻断

```
validation_status: INVALID
gate_decision: BLOCK
release_allowed: false
release_blocked_by: PERMIT_REQUIRED
error_code: E001
```

### 4.3 签名篡改阻断

```
validation_status: INVALID
gate_decision: BLOCK
release_allowed: false
release_blocked_by: PERMIT_INVALID
error_code: E003
```

---

## 5. Fail-Closed 验证

| 触发条件 | 期望行为 | 实际行为 | 状态 |
|----------|----------|----------|------|
| permit_token = null | E001 阻断 | E001 阻断 | ✅ |
| permit_token = "" | E001 阻断 | E001 阻断 | ✅ |
| 签名被篡改 | E003 阻断 | E003 阻断 | ✅ |
| 错误密钥验证 | E003 阻断 | E003 阻断 | ✅ |

---

## 6. 回放指针

可使用以下指针回放本次验证：

```
replay://RUN-20260218-PRERELEASE-001@2026-02-18T11:24:03Z
```

回放参数：
- repo_url: `https://github.com/local/NEW-GM`
- commit_sha: `4ea179d31a66fc2616f0cf953cd0e5c5c43d8ea8`
- at_time: `2026-02-18T11:24:03Z`
- run_id: `RUN-20260218-PRERELEASE-001`

---

## 7. 签名验证详情

本次验证使用**真实 HS256 验证器**（非 stub）：

```python
def real_hmac_verifier(permit: dict) -> bool:
    sig = permit.get("signature", {})
    if sig.get("algo") != "HS256":
        return False
    payload = {k: v for k, v in permit.items()
               if k not in ("signature", "revocation")}
    canonical = json.dumps(payload, sort_keys=True, separators=(',', ':'))
    expected_sig = hmac.new(
        key.encode('utf-8'),
        canonical.encode('utf-8'),
        hashlib.sha256
    ).digest()
    expected_b64 = base64.b64encode(expected_sig).decode('utf-8')
    return sig.get("value") == expected_b64
```

验证结果：
- 有效签名 → ✅ 通过
- 篡改后签名 → ❌ E003 阻断
- 错误密钥 → ❌ E003 阻断

---

## 8. 结论

### 8.1 验收状态

**ALL PASSED** — 所有验收项满足要求。

### 8.2 发布权限

**GRANTED** — Permit 签发成功，校验通过，可进入下一阶段。

### 8.3 剩余工作

| 项目 | 状态 |
|------|------|
| Permit Gate 实现 | ✅ 完成 |
| Permit Issuer 实现 | ✅ 完成 |
| 签名真验签 | ✅ 验证通过 |
| 非 dry-run 发布前验证 | ✅ 本次完成 |
| Gate 实现补全 | ⏳ 待完成 |
| 密钥管理系统集成 | ⏳ 待完成 |

---

## 9. 附件

- 运行单: `docs/2026-02-18/prerelease_non_dry_run_validation_run_sheet_v1.md`
- 契约: `docs/2026-02-18/contracts/permit_contract_v1.yml`
- Gate 实现报告: `docs/2026-02-18/permit_gate_implementation_report_v1.md`
- Issuer 实现报告: `docs/2026-02-18/permit_issuer_implementation_report_v1.md`

---

*Generated by CC-Code | Pre-release Validation v1 | 2026-02-18*
