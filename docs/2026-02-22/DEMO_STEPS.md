# GM-SkillForge Demo 步骤（5 分钟链路）

> 任务：P1-3 Demo 脚本
> 执行者：Kior-C
> 目标：5 分钟内复现完整演示链路

---

## 前置依赖

| 依赖 | 要求 | 检查命令 |
|---|---|---|
| Python | >= 3.10 | `python --version` |
| Node.js | >= 18 | `node --version` |
| 依赖任务 | T53, T54, T55 已完成 | 检查 `docs/2026-02-22/verification/T5*_execution_report.yaml` |

---

## 演示链路（3 步）

### Step 1：运行审计（约 30 秒）

```bash
# 进入项目根目录
cd D:/GM-SkillForge

# 运行 5 层静态审计（finance + legal 域，Top 3 技能）
python scripts/run_skill_5layer_audit.py --domains finance,legal --top-n 3
```

**预期输出：**
```
[audit] run_id=L5S-20260222XXXXXXXX-XXXXXXXX
[audit] evidence_ref=EV-L5S-20260222XXXXXXXX-XXXXXXXX
[audit] policy_version=v1.0
[audit] input_hash=...
[audit] result_hash=...
[audit] report_json=reports/skill-audit/finance_legal_top3_l5-static_2026-02-22.json
[audit] report_md=reports/skill-audit/finance_legal_top3_l5-static_2026-02-22.md
[audit] run_dir=reports/skill-audit/runs/L5S-20260222XXXXXXXX-XXXXXXXX
```

**产物验证：**
```bash
# 检查 JSON 报告已生成
ls reports/skill-audit/finance_legal_top3_l5-static_2026-02-22.json

# 检查 run_id 目录存在
ls reports/skill-audit/runs/
```

---

### Step 2：启动前端（约 1 分钟）

```bash
# 进入前端目录
cd D:/GM-SkillForge/ui/app

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

**预期输出：**
```
VITE v5.1.4  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

### Step 3：查看审计结果页（约 1 分钟）

1. 打开浏览器访问：`http://localhost:5173/audit/skill-audit`

2. **页面应展示：**
   - **总览卡片**：run_date, sample_size, gate_counts (PASS/WARN/FAIL), avg_overall_score
   - **技能明细表**：按 Gate/Score/Domain 排序，显示 domain, skill, gate, overall_score, est_tokens
   - **详情面板**：点击某行后显示 L1-L5 层级分数可视化
   - **结论限制区**：policy_version, policy_path, evidence_ref, limitations

3. **交互验证：**
   - 点击列头（Gate/Score/Domain/Tokens）可切换排序
   - 页面在桌面和移动端均正常显示（响应式布局）

---

## 快速验证命令汇总

```bash
# 一键验证审计脚本可执行
python scripts/run_skill_5layer_audit.py --domains finance,legal --top-n 3 && echo "AUDIT_OK"

# 验证产物存在
test -f reports/skill-audit/finance_legal_top3_l5-static_2026-02-22.json && echo "JSON_OK"
test -f reports/skill-audit/finance_legal_top3_l5-static_2026-02-22.md && echo "MD_OK"

# 验证前端构建（可选，需 node 环境）
cd ui/app && npm run build && echo "BUILD_OK"
```

---

## 常见问题

| 问题 | 解决方案 |
|---|---|
| `ModuleNotFoundError` | 确保在项目根目录运行，Python >= 3.10 |
| 前端白屏 | 检查 `npm install` 是否完成，浏览器控制台有无报错 |
| 无数据展示 | 确认 `reports/skill-audit/` 目录下有 JSON 文件 |
| 端口占用 | 使用 `npx vite --port 5174` 指定其他端口 |

---

## 验收标准

| 标准 | 验证方法 |
|---|---|
| 5 分钟内可复现 | 按上述步骤执行，计时验证 |
| 文档含完整命令 | 本文档包含所有必需命令 |
| 文档含预期输出 | 每步都有预期输出说明 |
| 前置依赖说明完整 | 前置依赖表列明所有要求 |

---

## Evidence Ref

- Evidence ID: `EV-T56-DEMO-STEPS`
- Kind: FILE
- Locator: `docs/2026-02-22/DEMO_STEPS.md`
- SHA256: 计算于文件落盘后

---

*Generated by Kior-C | Task T56 | 2026-02-22*
