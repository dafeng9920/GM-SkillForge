# REAL-TASK-002 正式落地执行前检查单

- 日期：`2026-03-10`
- 对象：`REAL-TASK-002`
- 用途：确认 `REAL-TASK-002` 是否具备转入第一次真实 `absorb + final accept` 的执行条件
- 性质：`pre-final-landing checklist`

## 一、当前定位

`REAL-TASK-002` 当前状态是：

- 已冻结样本
- `Compliance PASS`
- `patch_proposal_only`
- `not absorbed`
- `not final accepted`

因此本检查单的作用不是重复冻结样本，  
而是确认这单能否从“样本候选”转入“正式落地主单”。

## 二、主链前置检查

### Permit

- [ ] 已有明确 `Permit`
- [ ] `Permit` 已记录到正式材料
- [ ] `Permit` 不依赖口头默许

### 标准链

- [ ] 已确认本单适用唯一标准链：
  - `Permit -> dropzone -> pre_absorb_check -> absorb -> local accept -> final accept`
- [ ] 已确认不会跳过任一环节

## 三、任务边界检查

### 任务性质

- [ ] 本单仍保持低风险
- [ ] 本单仍保持单模块
- [ ] 本单不扩展为新业务修改
- [ ] 本单不转化为 remediation 单

### 目标对象

- [ ] 目标文件范围仍然清晰
- [ ] 不新增未批准文件
- [ ] 不引入与本单无关的改动

## 四、门禁检查

### pre_absorb_check

- [ ] 已明确本单需要跑 `pre_absorb_check`
- [ ] 已定义 `pre_absorb_check` 的通过标准
- [ ] 已定义失败时的阻断规则

### 状态口径

- [ ] 已明确 `proposal-only` 与 `absorbed` 的区别
- [ ] 已明确 `absorbed` 与 `final accepted` 的区别
- [ ] 已避免出现“未 absorb 先写 final accepted”

## 五、执行动作检查

### absorb

- [ ] 已定义什么动作算真实 `absorb`
- [ ] 已定义 absorb 完成后的证据记录方式
- [ ] 已定义 absorb 失败时的回滚口径

### local accept

- [ ] 已定义 `local accept` 的判定动作
- [ ] 已定义 `local accept` 的记录位置

### final accept

- [ ] 已定义 `final accept` 的最终判定人或判定规则
- [ ] 已定义 `final accept` 的文档化输出

## 六、证据链检查

- [ ] 已有完整任务标识
- [ ] 已有完整状态流转记录
- [ ] 已有 absorb 证据
- [ ] 已有 accept 证据
- [ ] 已有最终结论文档模板

## 七、环境与一致性检查

- [ ] 已确认执行环境口径不会污染本单最终结论
- [ ] 已说明 `/root/openclaw-box/...` 与 `/home/node/.openclaw/...` 的差异如何处理
- [ ] 已确认环境差异不会导致“形式 absorb / 实质不清”

## 八、通过标准

只有当以下三组同时满足时，`REAL-TASK-002` 才可从候选转入正式落地主单：

- [ ] 第一组：主链前置已明确
- [ ] 第二组：执行动作已定义
- [ ] 第三组：证据链与环境口径已清楚

## 九、当前结论模板

若未全部通过，应写成：

- `REAL-TASK-002 仍为正式落地主单候选，暂不进入真实 absorb`

若全部通过，才可写成：

- `REAL-TASK-002 具备进入第一次真实 absorb + final accept 的执行条件`

## 十、一句话

这份检查单的作用，是防止我们因为 `REAL-TASK-002` 看起来最合适，  
就跳过前置条件，直接把候选误当成正式落地主单。
