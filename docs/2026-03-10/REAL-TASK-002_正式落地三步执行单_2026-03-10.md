# REAL-TASK-002 正式落地三步执行单

- 日期：`2026-03-10`
- 对象：`REAL-TASK-002`
- 用途：把正式落地收敛为 `方案 / 执行 / 验收` 三步走
- 性质：`proposal-only master sheet`

## 一、方案

本单当前定位：

- `REAL-TASK-002` 是第一次真实 `absorb + final accept` 的优先候选
- 本单已于 `2026-03-10` 执行完成
- 当前执行确认结论为：`GO -> EXECUTED`

本步只确认三件事：

1. 能不能开跑  
   - 当前结论：`GO`
   - `REAL-TASK-002` 可以启动第一次真实 `absorb + final accept`

2. 谁负责判定  
   - 主控负责最终 `GO / NO-GO`
   - `pre_absorb_check`：由主控判定
   - `absorb`：由执行责任人记录
   - `local accept`：由本地验收责任人记录
   - `final accept`：由主控判定并记录

3. 缺什么就不准动  
   - 缺 `Permit`
   - 缺 `pre_absorb_check` 判定规则
   - 缺 `absorb / local accept / final accept` 记账责任
   - 缺环境分叉处理结论

当前已明确并补齐：

- `Permit = YES`
- `pre_absorb_check = 主控判`
- `PASS 标准 = 任务范围未漂移、状态词无污染、证据链完整、环境口径已声明`
- `absorb = 执行责任人记录`
- `local accept = 本地验收责任人记录`
- `final accept = 主控判定并记录`
- `env = 本次按 /home/node/.openclaw/... 环境口径算，/root/openclaw-box/... 仅作背景差异记录，不影响本次结论`

因此本单当前状态为：

- `GO`
- `executed`
- `absorbed`
- `final accepted`

## 二、执行

只有在 `GO` 之后，才允许进入执行。

执行顺序固定为：

1. `Permit`
2. `dropzone`
3. `pre_absorb_check`
4. `absorb`
5. `local accept`
6. `final accept`

执行阶段必须遵守：

- 不跳步
- 不提前写结论
- 不把 `proposal-only` 写成 `absorbed`
- 不把 `ABSORB_READY (proposal only)` 写成 `final accepted`

执行阶段最少要留下：

- `Permit` 记录
- `pre_absorb_check` 结果
- absorb 记录
- `local accept` 记录
- `final accept` 记录

### 本次执行结果

- `Permit`: 已按主控确认值执行
- `pre_absorb_check`: 已于 `2026-03-10` 实跑通过
- `absorb`: 已于 `2026-03-10` 实跑完成
- absorb 目标目录：`docs/REAL-TASK-002/`
- 吸收产物已落入主仓文档目录

## 三、验收

只有当以下事实全部成立时，才算验收通过：

- 已真实 `absorbed`
- 已完成 `local accept`
- 已完成 `final accept`
- 证据链完整

满足后，才允许写：

- `REAL-TASK-002` 已完成第一次真实主链落地验证

若其中任一项不成立，则验收结论必须保持：

- `未通过最终落地验收`
- `REAL-TASK-002 仍停留在候选或执行中状态`

### 本次验收结果

- 已真实 `absorbed`
- 已完成 `local accept`
- 已完成 `final accept`
- 证据链已落入 `docs/REAL-TASK-002/`
- 验收结论：`通过`
- `REAL-TASK-002` 已完成第一次真实主链落地验证

## 四、一句话

把事情压到最小，就是：

- 方案：先确认能不能跑
- 执行：按标准链一步步跑
- 验收：没完成真实 absorb 和 final accept 就不算过
