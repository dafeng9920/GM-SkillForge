# 当前主线任务 Top 3

- 日期：`2026-03-11`
- 前提：`T-ASM Final Gate = ALLOW`
- 当前阶段：`从“主链已装好”切换到“主链承接真实任务”`

## 一、Top 3 排序

### Top 1

`把新装好的本地主链接入真实开发任务`

目标：

- 不再把主链当样板
- 让下一批真实任务默认走：
  - `Permit -> pre_absorb_check -> absorb -> local_accept -> final_accept`

### Top 2

`把最小运行接口接入执行层`

目标：

- 让 `RunRequest / RunResult / ArtifactManifest` 从“已定义”变成“默认使用”
- 减少任务执行时的口径漂移

### Top 3

`把统一验证入口接入主线日常运行`

目标：

- 让 `scripts/unified_validation_gate.py` 从“存在”变成“默认门”
- 让后续任务不再分散调用多套验证方式

## 二、具体推进任务

### TASK-MAIN-01

名称：

- `真实任务接主链`

任务目标：

- 选定下一批真实开发任务中的一个低风险任务
- 用新主链完整承接一次
- 输出完整运行记录

预期产物：

- 一份真实任务执行单
- 一套 `RunRequest / RunResult / ArtifactManifest`
- 一次完整 `Permit -> pre_absorb_check -> absorb -> local_accept -> final_accept` 记录

### TASK-MAIN-02

名称：

- `运行接口接入执行层`

任务目标：

- 将 `core/runtime_interface.py` 接入当前执行流
- 明确哪些现有入口开始读写统一接口

预期产物：

- 接入清单
- 最小改造结果
- 兼容说明

### TASK-MAIN-03

名称：

- `统一验证入口接入主线`

任务目标：

- 将 `scripts/unified_validation_gate.py` 接入当前主线默认验证流程
- 明确何时调用、调用哪些模式

预期产物：

- 默认验证调用口径
- 接入说明
- 实际验证回传

## 三、给不同 Agent 的具体指令

### 1. 给 Execution Agent A

```text
你是 TASK-MAIN-01 的 Execution Agent。

任务目标：
把新装好的本地主链接入一个真实开发任务，完成一次真实承接。

你必须先读取：
- docs/2026-03-11/T-ASM_Final_Gate_裁决_2026-03-11.md
- docs/2026-03-11/当前主线任务_Top3_2026-03-11.md
- multi-ai-collaboration.md

你要完成：
1. 从当前仓内选出一个低风险、单模块、可审计的真实任务对象
2. 用新主链承接它：
   - Permit
   - pre_absorb_check
   - absorb
   - local_accept
   - final_accept
3. 产出完整执行记录

硬约束：
- 不得跳回“只是写方案”
- 不得选高风险任务
- 不得越权扩大文件范围

输出格式：
- status
- selected_real_task
- changed_files
- execution_summary
- evidence_refs
- risks
```

### 2. 给 Execution Agent B

```text
你是 TASK-MAIN-02 的 Execution Agent。

任务目标：
把 core/runtime_interface.py 真正接入当前执行层。

你必须先读取：
- core/runtime_interface.py
- core/RUNTIME_INTERFACE_README.md
- docs/2026-03-11/当前主线任务_Top3_2026-03-11.md
- multi-ai-collaboration.md

你要完成：
1. 盘点哪些现有执行入口还没使用统一接口
2. 选择最小改造路径
3. 让至少一条真实执行流开始使用：
   - RunRequest
   - RunResult
   - ArtifactManifest

硬约束：
- 不得大规模重构
- 不得破坏现有主链
- 不得引入无关抽象

输出格式：
- status
- changed_files
- integrated_entrypoints
- compatibility_notes
- evidence_refs
- risks
```

### 3. 给 Execution Agent C

```text
你是 TASK-MAIN-03 的 Execution Agent。

任务目标：
把 scripts/unified_validation_gate.py 接入主线日常运行。

你必须先读取：
- scripts/unified_validation_gate.py
- docs/2026-03-11/T-ASM_Final_Gate_裁决_2026-03-11.md
- docs/2026-03-11/当前主线任务_Top3_2026-03-11.md
- multi-ai-collaboration.md

你要完成：
1. 明确当前主线默认验证时机
2. 明确 unified_validation_gate 应在哪个阶段被调用
3. 落地最小接入
4. 给出主线默认验证调用方式

硬约束：
- 不得弱化 fail-closed
- 不得删除现有旁路验证能力
- 不得把验证体系扩成大改造

输出格式：
- status
- changed_files
- validation_integration_summary
- default_invocation
- evidence_refs
- risks
```

### 4. 给 Review Agent

```text
你是本轮主线推进任务的 Review Agent。

任务范围：
- TASK-MAIN-01
- TASK-MAIN-02
- TASK-MAIN-03

你只做审查，不做执行。

审查重点：
1. 是否真的在承接真实任务，而不是回到方案层
2. 是否越权改动
3. 是否破坏 T-ASM 已装好的主链
4. 是否缺少证据
5. 是否把接入动作扩成大重构

对每个任务分别输出：
- task_id
- decision: ALLOW / REQUIRES_CHANGES / DENY
- reasons
- evidence_refs
- required_changes
```

### 5. 给 Compliance Agent

```text
你是本轮主线推进任务的 Compliance Agent。

任务范围：
- TASK-MAIN-01
- TASK-MAIN-02
- TASK-MAIN-03

你按 fail-closed 做硬校验。

硬规则：
1. 不得绕过三权分立
2. 不得削弱 Permit / pre_absorb_check / absorb / local_accept / final_accept
3. 不得把“接口已定义”冒充“接口已接入”
4. 不得把“验证脚本存在”冒充“已进入主线默认调用”
5. 不得无证据宣称完成

对每个任务分别输出：
- task_id
- decision: PASS / FAIL
- violations
- evidence_refs
- required_changes
```

## 四、一句话

下一阶段不是继续证明“主链装好了”，  
而是让这条主链真的开始承接真实任务、真实接口、真实验证。
