# REAL-TASK-003-R1 修复任务定义

- 日期：`2026-03-09`
- 来源：`REAL-TASK-003 Compliance FAIL`
- 执行者：`云端 Codex`
- 模式：`PAYLOAD-FIRST + Controlled Bridge + Local Accept`
- 性质：`remediation / process repair`

## 一、任务目标

修复 `REAL-TASK-003` 暴露出的两个问题：

1. 执行边界破坏  
2. `completion_record` 状态口径污染

本单目标不是继续修改业务文档，而是产出：

- 修复后的 proposal-only 任务包定义
- 修复后的 `completion_record` 口径样板
- 修复说明与门禁再验证材料

## 二、为什么由云端 Codex 执行

本单属于：

- 首单流程修复
- 边界纪律修复
- 治理链校正

不属于：

- 高重复低风险吞吐任务

因此本单执行者明确为 `云端 Codex`，而不是 `小龙虾`。

## 三、修复范围

仅允许修复以下问题：

1. proposal-only 边界说明
2. `completion_record` 状态与结论口径
3. 若需要，可补 `test_report`/`manifest` 中的边界说明

## 四、硬禁止

- 不得扩大为新的业务修改任务
- 不得修改代码、脚本、contracts、skills
- 不得真实 absorb
- 不得把 remediation 写成 final accept

## 五、最低交付

- `blueprint.md`
- `risk_statement.md`
- `changes.diff`
- `test_report.md`
- `completion_record.md`
- `manifest.json`
- `resume_handoff.md`
- `checkpoint/state.yaml`

## 六、成功标准

- 明确恢复为 `dropzone-only proposal`
- `completion_record` 状态与当前门禁规则完全一致
- 不再出现 absorb-ready 前的口径污染
- 通过独立 `Review` 与独立 `Compliance`

## 七、分工规则

- `云端 Codex`：首单流程验证、治理修复、桥接/脚本/skill 安装、remediation
- `小龙虾`：低风险、强约束、重复性高的吞吐任务

## 八、一句话

`REAL-TASK-003-R1` 是治理修复单，不是加量单。
